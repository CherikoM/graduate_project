<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.project.dao.MainReleaseDAO">
    <resultMap id="mainReleaseResultMap" type="com.example.project.dataobject.MainReleaseDO">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="gmt_created" property="gmtCreated"/>
        <result column="gmt_modified" property="gmtModified"/>
    </resultMap>

    <select id="getMainReleaseById" resultMap="mainReleaseResultMap">
        <if test="id != null">
            select * from `main_release` where id = #{id}
        </if>
    </select>

    <select id="search" resultMap="mainReleaseResultMap">
        <if test="keyword != null">
            select *
            from `main_release`
            where
            match (`title`) against (#{keyword} in boolean mode)
        </if>
    </select>

    <insert id="addMainRelease" parameterType="com.example.project.dataobject.MainReleaseDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `main_release` (`title`)
        VALUES (#{title});
    </insert>

</mapper>